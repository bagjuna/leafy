# .github/workflows/fast-test-ci.yml

name: Backend Build and Push  # <-- 실제 CI 워크플로우의 name과 똑같이 만듭니다.

on:
  workflow_dispatch: # 수동 실행 버튼 추가

jobs:
  fake-build:
    runs-on: ubuntu-latest
    steps:
      - name: Fake build step
        run: echo "This is a fast test CI run. It will succeed in 5 seconds."
